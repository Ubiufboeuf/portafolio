---
interface Props {
  href?: string
  class?: string
}

const { href, class: className } = Astro.props
---

<a href={href} class={`${className} w-fit h-fit flex items-center justify-center gap-2 p-2 px-3 rounded-lg transition-colors`}>
  <slot />  
</a>